/**
 *
 *
 * @author: Bernhard Lukassen
 */

export default class DistributedIdentity {

    constructor({
                    handle
                } = {}) {
        Object.assign(this, { handle });
    }

}
